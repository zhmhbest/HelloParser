# Flex

## 文件结构

```c
/* 【定义部分】 */
%{
    // C头文件引用
    // 函数声明
%}
/* FLEX状态声明 */
%x COMMENT
/* 正则表达式定义（在规则部分用{}括起来即可调用） */
digits  ([0-9]+)
%%
/* 【规则部分】 */
正则表达式 | "字符串" {
    // yytext: 正则规则匹配到的字符串
    // yylval: 匹配的字符串所代表的值（需要赋值此全局变量）
    return 状态（告诉编译器，当前匹配的状态码）
}
%%
/* 【C函数部分】 */
```
